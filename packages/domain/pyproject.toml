[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ailab-domain"
version = "0.1.0"
description = "Shared domain schemas for AI Lab"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.8"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["ailab_domain*"]
